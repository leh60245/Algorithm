#include <bits/stdc++.h>
using namespace std;
using vi = vector<int>;


int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    int a, b;
    while (T--)
    {
        cin >> a >> b;
        cout << "yes\n";
    }
}
